import { Meta, Story, Canvas, Description, ArgsTable } from '@storybook/addon-docs/blocks';
import * as stories from './typography.stories.js';

<Meta title="Global Styles/Typography" />

# Typography

The following system font stack is set on the `<body>`

```css
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen,
    Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;

```

## Headers

Headings `<h1>` through `<h6>`. Headers will dynamically resize base on the screen width to prevent larger headings taking up too much space on smaller screens.

<Canvas>
    <Story story={stories.Headers} />
</Canvas>

## Standard Text

<Canvas>
    <Story story={stories.Paragraph} />
</Canvas>

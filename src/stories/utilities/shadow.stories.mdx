import { Meta, Story, Canvas, Description, ArgsTable } from '@storybook/addon-docs/blocks';
import * as stories from './shadow.stories.js';
import CSS from '../../lib/components/CSS';

<Meta title="Utilities/Shadow" component={CSS} argTypes={stories.argTypes} />

# Shadow

Shadow utilities are available to adjust the `box-shadow` of an element.
The `shadow` props takes a value between `0` and `24`.
The higher the value the higher the element will appear to be.

<Canvas>
    <Story story={stories.Shadow} name="Shadow Props" />
</Canvas>

<ArgsTable story="Shadow Props" />

## Examples

<Canvas>
    <Story story={stories.ShadowExamples} />
</Canvas>

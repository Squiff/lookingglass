import { Meta, Story, Canvas, Description, ArgsTable } from '@storybook/addon-docs/blocks';
import * as stories from './sizing.stories.js';
import Lookingglass from '../../lib/components/Lookingglass';

<Meta title="Utilities/Sizing" component={Lookingglass} argTypes={stories.argTypes} />

# Sizing

Utilities for setting width and height

<Canvas>
    <Story story={stories.SizingProps} name="Sizing Props" />
</Canvas>

<ArgsTable story="Sizing Props" />

## Usage

`width` and `height` props accept values:

-   `percentage` use a number to set percentage e.g. `width="25"` is `25%`
-   `viewport` use `vw` and `vh` for viewport units. e.g. `width="100vw"` for full screen width

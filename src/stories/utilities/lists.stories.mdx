import { Meta, Story, Canvas, Description, ArgsTable } from '@storybook/addon-docs/blocks';
import * as stories from './lists.stories.js';
import Lookingglass from '../../lib/components/Lookingglass';

<Meta title="Utilities/Lists" component={Lookingglass} argTypes={stories.argTypes} />

# List

List Utilities for bullet styles and position

<Canvas>
    <Story story={stories.ListProps} name="List Props" />
</Canvas>

<ArgsTable story="List Props" />

## Unordered and Ordered

List utilities can be used with `<ul>` and `<ol>` tags.

<Canvas>
    <Story story={stories.OrderedUnordered} name="Ordered and Unordered" />
</Canvas>

## Example Usage

<Canvas>
    <Story story={stories.ListExamples} />
</Canvas>

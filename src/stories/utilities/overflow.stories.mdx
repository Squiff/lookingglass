import { Meta, Story, Canvas, Description, ArgsTable } from '@storybook/addon-docs/blocks';
import * as stories from './overflow.stories.js';
import CSS from '../../lib/components/CSS';

<Meta title="Utilities/Overflow" component={CSS} argTypes={stories.argTypes} />

# Overflow

Overflow utilities are availble to set `overflow`, `overflow-y` and `overflow-x` CSS properties.

<Canvas>
    <Story story={stories.OverflowProps} name="Overflow Props" />
</Canvas>

<ArgsTable story="Overflow Props" />
